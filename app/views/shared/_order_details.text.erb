Bestell-ID: <%= @order.number %>
E-Mail: <%= @order.email %>

<% @order.addresses.each do |address| %>
<%= address.kind == 1 ? "FIRMENADRESSE" : "ADRESSE DER VERANTWORTLICHEN PERSON" %>

<%= address.kind == 1 ? "Firma: " + address.company : "" %>
Name: <%= address.first_name %> <%= address.surname %>
Straße: <%= address.street_name %> <%= address.street_number %>
Ort: <%= address.post_code %> <%= address.city %>
<%= address.kind == 1 ? "Ust. ID: " + address.tax_no : "" %>
<%= address.kind == 2 && !address.birthday.nil? ? "Geburtsdatum: " + address.birthday.long : "" %>

<% end %>

BESTELLPOSITIONEN

<% @order.order_lines.each do |line| %>
<%= line.kind == 1 ? "Ust. Registrierung" : "Ust. Deklaration" %> in <%= line.country_iso_code %>
<% end %>

PREIS: <%= @order.value / 100 %> EUR
